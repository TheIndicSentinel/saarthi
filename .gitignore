*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizard.settings.xml
.DS_Store
/build
**/build/
/build-logic/**/bin/
/captures
.externalNativeBuild
.cxx
local.properties
keystore/*.jks
keystore/*.keystore

# Secrets — never commit. Pack signing private key lives in .keys/ (offline);
# Firebase config is intentionally absent (no telemetry).
.keys/
*.pem
google-services.json

# Kotlin compiler transient session files
.kotlin/

# Local dev scratch — one-off regex-edit scripts and captured crash/debug logs
fix_*.py
scratchpad_*.py
debug_*.py
*_crash_log.txt
*_human_log.txt
