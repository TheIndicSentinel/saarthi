package com.saarthi.core.ui.theme

import androidx.compose.ui.graphics.Color

// Cyber-Vedic palette — deep space base with sacred gold accents
object SaarthiColors {
    // Backgrounds
    val DeepSpace = Color(0xFF080B14)
    val NavyDark  = Color(0xFF0D1321)
    val NavyMid   = Color(0xFF111827)
    val NavyLight = Color(0xFF1A2332)

    // Accent — sacred gold
    val Gold      = Color(0xFFD4A843)
    val GoldLight = Color(0xFFE8C76B)
    val GoldDim   = Color(0xFF8A6B2A)

    // Accent — teal/cyan pulse
    val CyberTeal  = Color(0xFF00D4AA)
    val CyberTealDim = Color(0xFF007A62)

    // Pack accent colors
    val KnowledgePurple = Color(0xFF7C4DFF)
    val MoneyGreen      = Color(0xFF00C853)
    val KisanEarth      = Color(0xFF8D6E63)
    val FieldBlue       = Color(0xFF1565C0)

    // Text
    val TextPrimary   = Color(0xFFF0F4FF)
    val TextSecondary = Color(0xFFB0BAD3)
    val TextMuted     = Color(0xFF5C6B8A)

    // Glass surfaces
    val GlassSurface      = Color(0x0DFFFFFF)  // 5% white
    val GlassBorder       = Color(0x1AFFFFFF)  // 10% white
    val GlassSurfaceHover = Color(0x1AFFFFFF)  // 10% white

    // Indian cultural palette
    val Saffron      = Color(0xFFFF6B2C)   // Kesari — fire, energy
    val Marigold     = Color(0xFFFFB300)   // Genda — celebration
    val TulsiGreen   = Color(0xFF2D7A4F)   // Tulsi — nature
    val Terracotta   = Color(0xFFB5541A)   // Mitti — earth

    // Status
    val Error   = Color(0xFFFF5252)
    val Success = Color(0xFF69F0AE)
    val Warning = Color(0xFFFFD740)
}
